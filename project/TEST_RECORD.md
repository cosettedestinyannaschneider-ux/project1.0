# 功能测试记录

## 当前成果收尾验收（2026-06-10）

### 收尾范围

- 不修改业务功能，不修改数据库结构，不执行 Git 暂存、提交或推送。
- 更新过期的 `PROJECT_STATUS.md`，使交接状态与阶段 A、B、C 实际完成情况一致。
- 重新启动当前后端并核对阶段 B、C 真实接口。
- 创建并验证当前数据库完整备份。
- 尝试补齐 HBuilderX H5 和微信小程序登录后验收。

### 数据库备份验证

- 备份文件：`database/ai_project_closeout_20260610.sql`
- 文件大小：167,201 字节
- SHA-256：`9151F8D7322EB620B31FA6DACD3EA0A9EB4458DF48C5235211B9E25A091D958A`
- 备份方式：MySQL 8.0.45 `mysqldump`，使用 `--single-transaction --routines --triggers --events`。

| 验证项 | 实际结果 | 状态 |
|---|---|---|
| 备份命令执行 | 正常完成并生成 SQL 文件 | 通过 |
| 建表语句 | 包含 21 个 `CREATE TABLE` | 通过 |
| 数据写入语句 | 包含 18 组 `INSERT INTO` | 通过 |
| 完整结束标记 | 包含 1 个 `Dump completed` 标记 | 通过 |
| 备份哈希 | 已计算并登记 SHA-256 | 通过 |
| 隔离测试痕迹 | 备份中不包含 `PHASE_CLOSEOUT_ADMIN_20260610` | 通过 |

### 当前成果真实回归

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 后端健康检查 | `GET /api/health` 返回成功 | 通过 |
| 阶段 B 用户、企业、部门列表 | 分别返回 12、5、3 条 | 通过 |
| 阶段 B 完整事务和保护闭环 | 见第二轮模块化整理回归，全部通过 | 通过 |
| 阶段 C 企业综合查询 | 返回 5 家企业 | 通过 |
| 阶段 C 企业聚合字段 | 返回隐患数、报告数、风险等级和报告集合 | 通过 |
| 阶段 C 状态筛选和风险排序 | 真实接口返回成功 | 通过 |
| 隔离测试管理员清理 | `PHASE_CLOSEOUT_ADMIN_20260610` 已精确删除 | 通过 |
| 隔离测试数据残留 | `PHASE_*` 测试用户为 0 | 通过 |

### 双端验收说明

- HBuilderX 项目和历史微信小程序编译产物可用，用户管理页、企业查询页及阶段 B 第二轮拆分组件均已生成。
- H5 自动页面点击仍被当前浏览器安全策略明确阻止，未绕过限制。
- 尝试通过 HBuilderX 桌面窗口补充验收时，自动化返回了错误窗口内容；为避免干扰当前 Codex 会话，已停止桌面点击操作。
- 登录后的 H5 与微信小程序最终点击流程仍需人工确认。

### 收尾后真实基线

- 企业 5、部门 3、用户 12、用户权限 40、隐患图片 14、排查报告 25、操作日志 245、迁移冲突 0。
- 本次收尾未修改数据库结构。
- **当前成果收尾无 DDL 变更。**

## 下一任务测试计划：后端阶段 D

- 详细范围见 `ADMIN_BACKEND_PLAN.md`。
- 测试知识库条目和分类 CRUD 联调、批量归档、分类删除保护。
- 测试 AI 模型配置 API Key 安全整改、激活切换事务、删除保护。
- 阶段 D 只联调 `pages/admin/knowledge.vue` 和 `pages/admin/model-config.vue`。

## 后端阶段 C 完成测试记录（2026-06-09）

### 数据库基线

- 真实库：企业 5、部门 5、用户 11、权限 35。
- 阶段 C 无 DDL 变更。

### 功能测试

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 企业综合查询返回聚合数据 | 隐患数/分析数/报告数/整改统计/主要隐患类型/报告列表完整 | 待后端重启后联调验证 | 待验证 |
| 企业综合查询筛选 | keyword/industry/type/status/date 筛选正确 | 待后端重启后联调验证 | 待验证 |
| 企业综合查询排序 | date/risk/name 排序正确 | 待后端重启后联调验证 | 待验证 |
| 企业档案更新（扩展） | 后端字段白名单更新，含 inspection_status/status/production_process | 待后端重启后联调验证 | 待验证 |
| CSV 导出 | UTF-8 BOM CSV，Excel 可打开 | 待后端重启后联调验证 | 待验证 |
| 隐患图片上传 enterprise_id 关联 | 上传时携带 enterprise_id | 前端已补充参数 | 通过 |
| 多图分析 enterprise_id 关联 | 分析时携带 enterprise_id | 前端已补充参数 | 通过 |
| enterprises.vue 真实接口 | 查询/更新/导出/下载全部替换模拟数据 | 代码已完成替换 | 通过 |
| 后端语法检查 | 4 个修改文件语法通过 | 全部通过 | 通过 |

### 前端联调与双端兼容

- `pages/admin/enterprises.vue` 已替换为真实接口调用。
- `pages/process/process.vue` 已补充 enterprise_id 传递。
- 未修改其他管理员页面，未实现阶段 D 及后续接口。
- HBuilderX 需重启后端进程以加载阶段 C 新代码后完成最终 HTTP 联调验证。

## 后端阶段 B 完成测试记录（2026-06-09）

### 数据库基线

- 开始前真实库：企业 5、部门 5、用户 11、权限 35、`phase_a_migration_conflicts` 0。
- 阶段 B 无 DDL 变更。
- 测试使用唯一前缀创建隔离企业、部门和用户，完成后已清理测试数据与测试日志。

### 功能、安全与事务测试

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 企业新增不写历史归属 | `enterprises.user_id` 保持空 | 保持 `NULL` | 通过 |
| 企业 -> 部门 -> 用户 -> 权限闭环 | 全流程成功且列表可推导企业部门 | 真实库执行成功 | 通过 |
| 用户新增事务写权限 | 用户与权限同时成功 | 写入 2 条测试权限 | 通过 |
| 用户更新事务替换权限 | 原权限完整替换 | 最终仅保留目标权限 | 通过 |
| 非白名单权限 | 后端拒绝且不产生用户 | 已拒绝，用户数为 0 | 通过 |
| 用户列表权限对象 | 返回企业、部门和权限对象 | 字段与权限对象正确 | 通过 |
| 部门删除保护 | 已分配用户时拒绝删除 | 已拒绝 | 通过 |
| 企业删除保护 | 存在部门时拒绝删除 | 已拒绝 | 通过 |
| 当前管理员保护 | 禁止禁用当前管理员 | 已拒绝 | 通过 |
| 用户删除 | 使用软禁用，列表默认仍展示可重新启用 | `status = disabled`，列表可见 | 通过 |
| 禁用用户重新启用 | 管理员通过 update 接口传 `status: "active"` 重新启用 | 用户状态恢复 active，列表正常展示 | 通过 |
| 组织管理操作日志 | 修改成功后写入 `action_logs` | 用户、权限、企业、部门日志均写入 | 通过 |
| SQL 参数化 | 阶段 B SQL 使用占位符 | 静态检查通过 | 通过 |
| 企业删除后端校验 | 前端不依赖内存数据预判，后端单点校验 | 存在部门时后端拒绝并提示明确错误 | 通过 |
| 用户状态筛选 | 前端可按正常/已禁用筛选用户 | 状态筛选正确切换 | 通过 |

### 前端联调与双端兼容

- `pages/admin/users.vue` 的用户、企业、部门读取与增删改已全部替换为真实接口。
- 未修改其他管理员业务页面，未实现阶段 C 及后续接口。
- HBuilderX 5.06 已识别该项目，并存在 `uni-app-frontend - Web` 与 `uni-app-frontend - 微信小程序` 两个运行目标。
- 页面继续使用 uni-app 的 `uni.request`、条件编译 API 地址、`view/input/scroll-view` 和响应式 `rpx`，未引入仅 H5 可用 API。
- 尝试接管 HBuilderX 执行双端运行验证时检测到用户正在操作，已停止自动化以避免干扰；真实设备或开发者工具的最终点击流程需在当前 HBuilderX 会话中人工确认。
- 当前端口 3000 在验证时仍运行修改前的旧后端进程，新企业路由尚未加载；旧进程测试产生的管理员禁用状态和测试日志已立即恢复并清理。完成 HBuilderX 最终联调前必须重启后端进程以加载阶段 B 新代码。
- 尝试在备用端口启动隔离后端进程执行 HTTP 路由测试时，运行环境禁止创建子进程；服务层、真实数据库事务和保护规则测试不受此限制，均已通过。
- 后端重启后 HTTP 联调复测通过：用户列表返回 11 条且包含权限对象，企业列表返回 5 条，部门列表返回 5 条，当前管理员禁用请求被明确拒绝；复测后数据库数量和管理员状态均未变化。
- 尝试自动检查当前 H5 浏览器页面时，Windows 无法可靠识别当前浏览器 URL，界面自动化被安全机制停止；未绕过限制或继续操作浏览器。
- 修复组织管理弹窗内企业、部门删除点击无响应：不再在双层业务弹窗内调用 `uni.showModal`，改为页面内高层级确认弹窗，并直接展示后端关联保护错误。
- 删除保护 HTTP 复测：有关联部门的企业返回“企业下存在部门，禁止删除”；已分配用户的部门返回“已分配用户的部门禁止删除”。
- 修复禁用用户重新启用时未携带原权限对象的问题，避免事务更新时清空用户权限。
- 优化部门删除规则：正常或锁定用户仍阻止删除；仅关联禁用用户时，事务内解除禁用用户部门关联、删除部门并记录日志。
- 部门删除事务测试通过：正常用户阻止删除、仅禁用用户允许删除并解除关联、日志写入失败时完整回滚。

### 阶段 B 重启后完整回归（2026-06-09）

- 用户、企业、部门真实列表接口正常，用户列表包含权限对象。
- 隔离 HTTP 闭环通过：新增企业、部门、用户和权限，更新并替换权限，禁用用户，删除仅关联禁用用户的部门，删除无业务数据企业。
- 删除保护通过：正常用户阻止部门删除，当前管理员禁止禁用，有隐患图片或排查报告的企业禁止删除。
- 组织约束通过：企业重命名、部门重命名、企业内同名部门拒绝、有效用户部门禁止跨企业移动、非管理员访问拒绝。
- 权限与事务通过：非法部门或权限请求不会创建用户，用户权限更新执行完整替换。
- 操作日志通过：用户权限更新、用户禁用、部门删除和企业删除日志均成功写入；隔离测试日志已清理。

## 阶段 B 独立模块化整理回归（2026-06-09）

### 整理范围

- 后端拆分管理员鉴权、统一业务错误响应以及用户、企业、部门路由。
- 前端拆分用户组织管理 API 客户端、筛选配置、表单默认值和用户列表纯展示逻辑。
- 页面模板、样式、接口路径和阶段 B 业务规则保持不变。
- 阶段 B 模块化整理无 DDL 变更。

### 回归结果

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 后端入口及拆分路由语法检查 | `server.js`、中间件及 4 个路由模块均通过 | 通过 |
| 用户、企业、部门列表 | 拆分后的真实路由正常返回 | 通过 |
| 企业 -> 部门 -> 用户 -> 权限闭环 | HTTP 隔离闭环成功 | 通过 |
| 用户新增和更新事务 | 权限新增与完整替换成功 | 通过 |
| 用户软禁用 | 禁用后仍可查询 | 通过 |
| 仅关联禁用用户的部门删除 | 自动解绑禁用用户并成功删除部门 | 通过 |
| 空企业删除 | 部门删除后企业成功删除 | 通过 |
| 当前管理员禁用与降权保护 | 后端明确拒绝 | 通过 |
| 有正常用户部门删除保护 | 后端明确拒绝 | 通过 |
| 有部门企业删除保护 | 后端明确拒绝 | 通过 |
| 阶段 B 操作日志 | 本轮闭环写入 9 条对应日志 | 通过 |
| H5 与微信小程序命令行构建 | 项目由 HBuilderX 管理，本地无可调用 `uni` CLI | 未执行 |
| 双端兼容静态检查 | 保留 uni-app API、基础组件、响应式单位和原模板样式 | 通过 |

### 测试数据说明

- 临时企业和部门已通过阶段 B 接口清理。
- 临时禁用用户及本轮对应测试日志已在精确 ID 校验后使用数据库事务清理。
- H5 和微信小程序最终渲染点击回归仍应在 HBuilderX 的两个运行目标中执行。

## 阶段 B 模块化整理双端验收（2026-06-09）

### 验收环境

- 后端：`http://127.0.0.1:3000`，拆分后的路由代码已加载。
- H5：HBuilderX Web 运行目标，服务监听 `http://localhost:5173`。
- 微信小程序：微信开发者工具 Stable `2.01.2510290`，基础库 `3.15.2`。
- 验收前后数据库保持：企业 5、部门 3、用户 12、用户权限 40。

### 后端与 H5 验收

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 拆分后路由语法检查 | 入口、中间件、用户/企业/部门路由全部通过 | 通过 |
| 用户、企业、部门真实列表 | 分别返回 12、5、3 条数据 | 通过 |
| 企业归属推导 | 有部门用户均通过部门返回企业信息 | 通过 |
| 用户权限返回 | 普通用户均返回权限对象 | 通过 |
| 当前管理员禁用保护 | 返回“禁止禁用当前登录管理员” | 通过 |
| 当前管理员降权保护 | 返回“禁止禁用当前管理员或取消当前管理员身份” | 通过 |
| 正常用户部门删除保护 | 返回“部门存在正常或锁定用户，禁止删除” | 通过 |
| 企业关联删除保护 | 返回“企业下存在部门，禁止删除” | 通过 |
| H5 服务响应 | `localhost:5173` 返回 HTTP 200，包含应用挂载节点 | 通过 |
| H5 页面点击回归 | 自动浏览器验收被当前安全策略阻止，未绕过限制 | 待人工确认 |

### 微信小程序实际页面验收

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 用户管理页面加载 | 展示真实统计：用户 12、检查员 8、管理员 4、已禁用 2 | 通过 |
| 移动端用户卡片 | 用户组织、状态、编辑及启用/禁用按钮正常展示 | 通过 |
| 已禁用状态筛选 | 点击后仅展示 2 个已禁用用户 | 通过 |
| 新增用户弹窗 | 点击后正常打开，表单和权限区域完整展示 | 通过 |
| 企业与部门管理弹窗 | 从新增用户弹窗内正常打开 | 通过 |
| 企业删除点击 | 正常打开页面内“确认删除”层，不再点击无响应 | 通过 |
| 删除确认取消 | 正常关闭确认层，未产生数据修改 | 通过 |
| 微信端生成产物 | `unpackage/dist/dev/mp-weixin` 于本次验收时成功更新 | 通过 |
| 微信控制台 | 存在 1 条微信基础库 `WAServiceMainContext` timeout，未指向项目源码 | 观察项 |

### 验收结论

- 阶段 B 后端模块化整理验收通过。
- 微信小程序真实页面和关键只读交互验收通过。
- H5 服务与运行目标正常，最终页面点击流程需由用户在当前 HBuilderX Web 页面人工确认。
- 本次验收未新增、更新或删除业务数据。
- **阶段 B 模块化整理验收无 DDL 变更。**
- 受保护企业当前部门数为 0，但仍关联隐患图片和排查报告，因此企业删除接口返回“企业存在隐患图片或排查报告，禁止删除”，符合企业档案保护规则。
- 回归测试完成后无 `PHASE_B_HTTP_*`、`PHASE_B_UPDATE_*` 测试用户、企业或测试日志残留。

## 阶段 B 第二轮独立模块化整理回归（2026-06-09）

### 整理范围

- 新增 `useOrganizationManagement.js`，拆出企业与部门列表、组织管理状态、增删改和删除确认操作。
- 新增 `useUserEditor.js`，拆出用户新增、编辑、权限、启用和禁用操作。
- 用户编辑弹窗、企业与部门管理弹窗、组织删除确认层分别拆为独立组件。
- 页面直接挂载业务组件并使用显式事件，未引入多层业务插槽。
- 接口路径、请求参数、响应结构、业务规则、页面交互和数据库结构保持不变。
- **阶段 B 第二轮模块化整理无 DDL 变更。**

### 开始前真实基线

- 企业 5、部门 3、用户 12、用户权限 40、隐患图片 14、排查报告 25、迁移冲突 0。
- 当前管理员 ID 1 状态为 `active`。
- 用户、企业、部门真实列表分别返回 12、5、3 条。
- 后端端口 `3000` 与 HBuilderX Web 端口 `5173` 均处于监听状态。

### 逐模块静态与编译回归

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 组织管理 composable 拆分 | JavaScript 语法检查通过，微信用户页产物自动更新 | 通过 |
| 用户编辑 composable 拆分 | JavaScript 语法检查通过，当前管理员保护和权限返回保持不变 | 通过 |
| 用户编辑弹窗组件拆分 | 独立 JS/WXSS 产物生成，真实列表数量不变 | 通过 |
| 企业与部门管理弹窗组件拆分 | 独立 JS/WXSS 产物生成，企业与部门汇总关系不变 | 通过 |
| 组织删除确认层组件拆分 | 独立 JS/WXSS 产物生成，企业业务关联删除保护提示不变 | 通过 |
| 页面死样式清理 | 已迁移弹窗样式从页面移除，HBuilderX 微信产物重新生成 | 通过 |
| Git 空白与语法检查 | 本轮新增 composable 和组件检查无错误 | 通过 |

### 真实阶段 B HTTP 完整回归

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 用户、企业、部门真实列表 | 返回 12、5、3 条 | 通过 |
| 企业新增与重命名 | 新增后重命名成功 | 通过 |
| 部门新增与重命名 | 新增后重命名成功 | 通过 |
| 用户新增与权限保存 | 用户和初始权限事务保存成功 | 通过 |
| 用户更新与权限完整替换 | 原权限移除，新权限集合生效 | 通过 |
| 正常用户部门删除保护 | 返回“部门存在正常或锁定用户，禁止删除” | 通过 |
| 有部门企业删除保护 | 返回“企业下存在部门，禁止删除” | 通过 |
| 当前管理员禁用保护 | 返回“禁止禁用当前登录管理员” | 通过 |
| 当前管理员降权保护 | 返回“禁止禁用当前管理员或取消当前管理员身份” | 通过 |
| 非白名单权限事务回滚 | 请求被拒绝且未创建用户 | 通过 |
| 用户软禁用 | 用户状态成功改为禁用 | 通过 |
| 仅关联禁用用户的部门删除 | 自动解除关联并成功删除 | 通过 |
| 空企业删除 | 部门删除后企业成功删除 | 通过 |
| 隔离测试数据清理 | 测试用户、企业、部门和权限均无残留 | 通过 |

### HBuilderX 与双端验收

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| HBuilderX Web 运行目标 | 运行目标存在，端口 `5173` 正常监听 | 通过 |
| HBuilderX 微信小程序运行目标 | 运行目标存在，用户页及三个独立组件产物成功更新 | 通过 |
| HBuilderX 热更新 | 显示 `pages/admin/users.vue` 样式 HMR 更新记录 | 通过 |
| H5 页面点击回归 | 当前浏览器安全策略明确阻止访问 `localhost:5173`，未绕过限制 | 待人工确认 |
| 微信小程序页面点击回归 | 微信开发者工具正常加载最新产物，但当前停留在登录页且无可用登录凭据 | 待人工确认 |

### 数据与问题说明

- 回归结束后业务基线保持：企业 5、部门 3、用户 12、用户权限 40、隐患图片 14、排查报告 25、迁移冲突 0。
- 首次 HTTP 测试脚本误将新增接口 ID 按 `data.id` 读取，导致后续请求使用空 ID；遗留空企业已按精确 ID 清理，修正后完整闭环全部通过。
- 测试日志清理首次错误使用日志数量作为 ID 边界，导致近期管理员测试日志总数由本轮开始时 257 条降至 245 条；业务表数据未受影响，后续停止使用非精确日志范围清理。
- 本轮未修改后端代码、其他管理员页面或数据库结构。
- **阶段 B 第二轮模块化整理无 DDL 变更。**

## 后端阶段 A：数据库迁移与组织关系（2026-06-09）

### 测试环境与迁移前盘点

- 数据库：MySQL 8.0.45，目标库 `ai_project`
- 迁移脚本：`database/migrations/20260609_admin_backend_phase_a.sql`
- 验证脚本：`database/migrations/20260609_admin_backend_phase_a_verify.sql`
- 完整备份：`database/ai_project_before_phase_a_20260609.sql`，97,823 字节
- 迁移前共有企业 5 条、部门 3 条、用户 11 条、隐患图片 16 条、排查报告 34 条。

### 迁移与约束验证

| 测试用例 | 实际结果 | 状态 |
|---|---|---|
| 临时克隆库首次迁移 | 完整执行成功 | 通过 |
| 临时克隆库重复迁移 | 第二次执行成功，无重复数据或结构错误 | 通过 |
| 真实库首次及重复迁移 | 两次执行均成功，`enterprises.user_id` 保留并改为可空 | 通过 |
| 企业 1:N 部门 | 5 个企业各创建 1 个默认部门，无孤立部门 | 通过 |
| 部门 1:N 用户 | 5 个历史 owner 用户迁入对应默认部门，无孤立用户部门 | 通过 |
| 企业与部门名称唯一约束 | 企业重复数 0，企业内重复部门数 0 | 通过 |
| 企业用户历史外键 | `enterprises.user_id` 到用户表的级联外键已移除 | 通过 |
| 用户权限持久化 | 7 个普通用户共 35 条权限，无重复、无孤立权限 | 通过 |
| 新增字段与表 | `inspection_status`、`provider`、`backup_records` 均验证通过 | 通过 |
| 图片企业关联补齐 | 16 条图片均有企业关联 | 通过 |
| 报告企业关联补齐 | 24 条已有或成功补齐；10 条无法归属的测试报告经确认后删除 | 通过 |
| 冲突数据保护 | 2 条图片关联冲突先记录且未覆盖，经确认属于测试数据后删除 | 通过 |

### 阶段 A 遗留数据确认

| 冲突类型 | 数量 | 处理状态 |
|---|---:|---|
| `HAZARD_ENTERPRISE_MISMATCH` | 2 | 已确认属于测试数据并删除 |
| `REPORT_ENTERPRISE_UNRESOLVED` | 10 | 已确认属于测试数据并删除 |

### 阶段 A 测试数据清理

- 使用单个数据库事务，按 `phase_a_migration_conflicts` 精确删除 10 条测试报告和 2 条测试图片。
- 删除前确认目标数据均未关联 `inspection_report_images`。
- 同步删除对应 12 条迁移冲突记录。
- 未删除磁盘上的历史上传文件；数据库迁移前完整备份和快照表继续保留。

### 本阶段变更说明

- 接口文档：现有部门接口已适配 `enterprise_id`；阶段 B 再完成组织与权限管理完整联调。
- 数据库 DDL：已完成阶段 A 组织关系、权限、排查状态、模型服务商和备份记录结构。
- 后端代码：仅调整数据库初始化器，不修改管理员业务接口。
- 前端代码：无变更。

### 阶段 A 后端结构兼容修复

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 企业列表包含 `user_id IS NULL` 企业 | 新组织主数据不因历史用户字段为空而丢失 | `enterpriseDal.findAll` 使用 `LEFT JOIN`，事务测试可见 | 通过 |
| 创建部门写入企业 ID | 满足 `departments.enterprise_id NOT NULL` | DAL 与管理员接口均强制接收 `enterprise_id` | 通过 |
| 用户推导所属企业 | 通过用户部门关系返回企业 | `findByUserOrganization` 查询验证通过 | 通过 |
| 企业档案更新 | 按推导出的企业 ID 更新，不再写入历史 `user_id` | `updateById` 事务测试通过 | 通过 |
| AI 分析企业回退 | 未显式传企业 ID 时按用户部门推导 | 路由静态检查通过 | 通过 |
| 排查状态类型一致 | 初始化器与完整 schema 均使用相同枚举 | 类型检查通过 | 通过 |
| 管理员部门列表筛选 | 支持按 `enterprise_id` 查询 | DAL 查询验证通过 | 通过 |
| 部门组织关系保护 | 有用户的部门禁止删除和跨企业移动 | 后端保护逻辑静态检查通过 | 通过 |
| 设计文档表数量 | 与完整 schema 的 14 张业务表一致 | 已更新为 14 张业务表 | 通过 |

## 下一阶段测试计划

- 下一阶段进入管理员后端与数据库实施，完整顺序见 `ADMIN_BACKEND_PLAN.md`。
- 首个后端任务只验证数据库迁移与组织关系，不进行全部接口联调。
- 必须测试迁移前备份、旧数据迁移、企业与部门关系、用户归属、权限持久化、外键和唯一约束。

## 管理员工作台首页

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 管理员进入工作台 | 正常展示统计卡片和 7 个系统管理入口 | 待运行环境验证 | 待验证 |
| 普通用户访问工作台 | 自动返回登录页面 | 待运行环境验证 | 待验证 |
| H5 宽屏访问 | 展示顶部导航、左侧菜单、统计卡片和管理列表 | 待运行环境验证 | 待验证 |
| H5 小屏访问 | 自动切换为移动端三列统计卡片和管理列表 | 待运行环境验证 | 待验证 |
| 微信小程序访问 | 展示移动端布局且无横向溢出 | 待运行环境验证 | 待验证 |
| 点击系统管理入口 | 跳转到对应的现有管理员功能页面 | 待运行环境验证 | 待验证 |
| 点击退出登录 | 二次确认后清理登录状态并返回登录页 | 待运行环境验证 | 待验证 |

## 本次变更说明

- 接口文档：管理员工作台统计数据暂时使用前端模拟数据。
- 数据库 DDL：无变更。
- 后端代码：无变更。

## 企业状态选择器跨端兼容修复（2026-06-10）

### 问题与修复

- H5 企业状态筛选原生 `picker` 展开后内容空白，编辑弹窗中的排查状态和档案状态点击无响应。
- 微信小程序企业状态筛选将对象选项直接显示为 `[object Object]`。
- 企业卡片、详情和分类区域直接显示 `pending`、`inspecting` 等数据库英文枚举。
- 已将三个状态选择入口改为紧贴触发框展开的自定义下拉框，H5 与微信小程序均不再使用遮罩弹窗。
- 排查状态统一按 `pending / inspecting / rectification / completed` 保存和筛选，界面显示为“待排查 / 排查中 / 整改中 / 已完成”。

### 回归记录

| 测试项 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 微信小程序状态选择器编译 | 状态筛选和编辑状态不再生成对象型原生 `picker` 或遮罩弹窗 | HBuilderX 自动编译通过，生成 WXML 中仅保留行业、类型、风险三个字符串 `picker`，三个状态入口均生成锚定下拉框 | 通过 |
| 微信小程序状态选项显示 | 不再出现 `[object Object]`，显示中文选项 | 生成代码包含中文状态选项和独立状态选择层，不包含 `[object Object]` | 通过 |
| 排查状态显示映射 | 卡片、详情、分类和编辑框显示中文，接口值保持英文枚举 | 静态检查和微信生成代码检查通过 | 通过 |
| 阶段 B 企业查询 HTTP 回归 | 企业查询接口正常返回真实数据 | `POST /api/admin/enterprises/query` 返回 `code=0`，状态筛选返回 4 家正常企业 | 通过 |
| H5 状态选择交互 | 全部状态、排查状态、档案状态点击后在触发框下方显示中文下拉框并可选择 | 用户在当前运行页面确认交互可用 | 通过 |
| 微信小程序状态选择交互 | 三个状态入口在触发框下方显示中文下拉框并可选择 | 用户确认当前交互可用，HBuilderX 微信产物已更新 | 通过 |

### 变更说明

- 本次仅修改企业查询页状态选择和状态显示逻辑，不修改接口路径、业务规则、数据库行为或其他管理员页面。
- 阶段 B 第二轮模块化整理无 DDL 变更。

## 阶段 B 第二轮模块化整理最终收尾（2026-06-10）

### 自动回归结果

| 测试项 | 实际结果 | 状态 |
|---|---|---|
| 模块化 JavaScript 语法检查 | `useOrganizationManagement.js`、`useUserEditor.js`、`useUserManagement.js`、组织 API 和三组后端路由检查通过 | 通过 |
| 用户、企业、部门真实列表 | HTTP 返回 `12 / 5 / 3` 条，接口路径与返回行为不变 | 通过 |
| 企业综合查询 | 正常档案状态筛选返回 4 家企业 | 通过 |
| 当前管理员禁用保护 | 返回“禁止禁用当前登录管理员” | 通过 |
| 企业业务关联删除保护 | 返回“企业存在隐患图片或排查报告，禁止删除” | 通过 |
| 数据库业务基线 | 企业 5、部门 3、用户 12、权限 40、图片 14、报告 25、操作日志 250、迁移冲突 0 | 通过 |
| 数据库关系完整性 | 孤立部门、孤立用户组织关系、孤立权限、非法排查状态均为 0 | 通过 |
| 隔离测试数据残留 | `PHASE_*` 测试用户为 0 | 通过 |
| H5 运行目标 | `http://localhost:5173` 返回 200，Vite 应用入口可用 | 通过 |
| 微信小程序生成产物 | 用户管理三个拆分组件及企业页 JS/WXML/WXSS 已由 HBuilderX 更新 | 通过 |
| 企业状态锚定下拉框 | 生成产物不包含状态遮罩弹窗或 `[object Object]`，用户确认交互可用 | 通过 |

### 最终结论

- 阶段 B 第二轮独立模块化整理已完成最终收尾。
- 接口路径、业务规则、数据库行为和其他管理员页面未发生变化。
- 未执行 Git 暂存、提交或推送。
- **阶段 B 第二轮模块化整理无 DDL 变更。**

## 第三阶段：企业查询、操作日志、报告模板与数据备份 UI

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 企业数据组合筛选 | 可按关键词、行业和排查日期筛选企业 | 静态逻辑检查通过 | 通过 |
| 企业数据排序与详情 | 可按日期或名称排序，并展开企业统计和报告 | 静态逻辑检查通过 | 通过 |
| 企业高级筛选 | 可按企业类型、风险等级和企业状态进一步筛选 | 静态逻辑检查通过 | 通过 |
| 企业完整档案详情 | 展示地址、规模、生产工艺、联系人、检查人员和状态 | 静态逻辑检查通过 | 通过 |
| 企业隐患整改详情 | 展示隐患总数、待整改、已整改、整改率和主要隐患类型 | 静态逻辑检查通过 | 通过 |
| 企业信息编辑保存 | 可编辑企业档案、排查状态和档案状态，保存后列表立即更新 | 静态逻辑检查通过 | 通过 |
| 企业分类快速查找 | 可按排查状态或所属区域分类查看企业 | 静态逻辑检查通过 | 通过 |
| 企业专属隐患档案 | 展示企业关联的隐患图片、分析结果和报告记录 | 静态逻辑检查通过 | 通过 |
| 企业 Excel 导出入口 | 点击后创建模拟导出任务 | 静态逻辑检查通过 | 通过 |
| 操作日志搜索与模块筛选 | 可按用户、操作、详情和模块筛选 | 静态逻辑检查通过 | 通过 |
| 报告模板新增与编辑 | 可选择 DOCX 文件并维护模板信息 | 静态逻辑检查通过 | 通过 |
| 默认模板限制 | 可切换默认模板，默认模板禁止删除 | 静态逻辑检查通过 | 通过 |
| 手动数据库备份 | 点击后显示执行状态并新增模拟备份记录 | 静态逻辑检查通过 | 通过 |
| 自动备份策略 | 可选择周期并保存模拟设置 | 静态逻辑检查通过 | 通过 |
| 四个页面微信端适配 | 使用单列卡片、直接事件绑定且无多层业务插槽 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |

### 第三阶段变更说明

- 企业数据查询、操作日志、报告模板和数据备份已接入管理员公共框架。
- 第三阶段模拟数据统一放入 `common/admin-mock-data.js`。
- 数据库 DDL：无变更。
- 后端代码：无变更。

## 第二阶段：知识库管理与 AI 模型配置 UI

### 功能测试用例

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 知识库页面接入管理员公共框架 | 管理员可进入页面，当前菜单高亮，非管理员被拦截 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |
| 知识文档关键词搜索 | 可按标题、说明和文件名筛选 | 静态逻辑检查通过 | 通过 |
| 知识分类筛选 | 分类标签和选择器均可筛选文档 | 静态逻辑检查通过 | 通过 |
| 新增知识文档 | 校验标题和文件，新增后立即显示 | 静态逻辑检查通过 | 通过 |
| 编辑知识文档 | 可修改标题、说明、分类和文件 | 静态逻辑检查通过 | 通过 |
| 单条与批量删除文档 | 二次确认后删除模拟数据 | 静态逻辑检查通过 | 通过 |
| 分类管理 | 可新增、编辑分类，关联文档分类禁止删除 | 静态逻辑检查通过 | 通过 |
| AI 模型配置概览 | 显示当前启用模型、参数和全部配置 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |
| 新增与编辑模型配置 | 必填项校验正确，编辑时 API Key 可留空 | 静态逻辑检查通过 | 通过 |
| 切换当前模型 | 二次确认后仅一个模型处于启用状态 | 静态逻辑检查通过 | 通过 |
| 删除模型配置 | 当前启用模型禁止删除，其他模型二次确认后删除 | 静态逻辑检查通过 | 通过 |
| H5 与微信端响应式布局 | 宽屏双列卡片，小屏单列卡片和底部弹窗 | 待 HBuilderX 双端运行验证 | 待验证 |
| 微信端知识库操作区 | 页面标题不重复显示，分类管理和上传文档按钮横向紧凑排列 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |
| 微信端知识库上传弹窗 | 点击上传后显示底部抽屉和遮罩，表单不再铺入页面滚动内容 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |
| 微信端 AI 模型配置弹窗 | 添加和编辑模型时显示独立底部抽屉 | Vue 静态编译通过，待 HBuilderX 运行验证 | 待验证 |
| 微信端知识库按钮点击 | 分类管理与上传文档按钮点击后更新弹窗状态并展示对应弹窗 | 已移除多层组件插槽事件，Vue 静态编译通过 | 待 HBuilderX 验证 |
| 微信端知识文档列表 | 统计数量不为零时正常展示对应文档卡片 | 已改为业务页面直接渲染，Vue 静态编译通过 | 待 HBuilderX 验证 |

### 接口联调状态

| 模块 | 当前状态 | 后续工作 |
|---|---|---|
| 知识库列表、上传、编辑、单条删除 | 前端使用模拟数据 | 按 `API_DOC.md` 替换页面数据方法 |
| 知识库批量删除 | 后端无批量接口 | 按 `BACKEND_TODO.md` 新增批量归档接口 |
| 知识分类管理 | 前端使用模拟数据 | 接入现有分类 CRUD，并补充后端关联校验 |
| AI 模型配置 | 前端使用模拟数据 | 接入现有接口并先完成 API Key 安全整改 |

### 第二阶段变更说明

- 删除管理员公共框架底部版权信息。
- 新增通用页面标题、通用响应式弹窗和知识条目卡片组件。
- 知识库管理与 AI 模型配置页面已接入管理员公共框架。
- 修复微信小程序中弹窗位于 `scroll-view` 内导致固定定位失效的问题，业务弹窗已移动到公共滚动框架外层。
- 移动端页面标题区域仅保留紧凑操作按钮，避免与蓝色顶部导航重复展示。
- 修复微信小程序多层组件插槽导致知识库按钮事件和文档列表渲染失效的问题；知识库按钮、条目和弹窗改为业务页面直接渲染。
- AI 模型配置页同步移除操作按钮和弹窗的多层插槽依赖，避免相同微信端问题。
- Vue 单文件组件静态解析检查：通过。
- 数据库 DDL：无变更。
- 后端代码：无变更。

## 第一阶段：管理员公共框架与用户管理 UI

### 功能测试用例

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 管理员公共框架鉴权 | 管理员正常进入页面，非管理员返回登录页 | 待 HBuilderX 运行验证 | 待验证 |
| H5 管理员公共导航 | 显示顶部栏、左侧菜单并高亮当前页面 | 待 HBuilderX 运行验证 | 待验证 |
| 微信端管理员公共导航 | 显示返回式蓝色顶部栏，不显示左侧菜单 | 待 HBuilderX 运行验证 | 待验证 |
| 工作台接入公共框架 | 统计卡片与 7 个管理入口保持正常 | 静态检查通过 | 通过 |
| 用户统计概览 | 正确展示用户总数、检查员数和管理员数 | 静态检查通过 | 通过 |
| 用户关键词搜索 | 可按用户名、企业或部门筛选用户 | 静态检查通过 | 通过 |
| 用户角色筛选 | 可切换全部用户、检查员和管理员 | 静态检查通过 | 通过 |
| H5 用户列表 | 宽屏显示用户表格和操作入口 | 待 HBuilderX 运行验证 | 待验证 |
| 微信端用户列表 | 小屏显示用户卡片且无横向溢出 | 待 HBuilderX 运行验证 | 待验证 |
| 新增用户 | 校验用户名、初始密码并创建本地模拟用户 | 静态检查通过 | 通过 |
| 编辑用户 | 可修改账号、所属企业、所属部门、角色和权限 | 静态检查通过 | 通过 |
| 权限全选与逐项选择 | 普通用户权限卡片可全选或逐项切换 | 静态检查通过 | 通过 |
| 删除用户 | 二次确认后删除，且不能删除当前管理员 | 静态检查通过 | 通过 |
| 浏览器选择所属部门 | 点击所属部门后展开自定义部门列表并可选择 | 静态检查通过 | 通过 |
| 新增所属部门 | 部门管理弹窗可新增不重名部门 | 静态检查通过 | 通过 |
| 重命名所属部门 | 修改部门名称后同步更新已分配用户展示名称 | 静态检查通过 | 通过 |
| 删除所属部门 | 仅允许删除未分配用户的部门，并进行二次确认 | 静态检查通过 | 通过 |
| 企业与部门级联选择 | 选择企业后仅展示该企业下的部门，切换企业后清空部门 | 静态检查通过 | 通过 |
| 企业管理模拟交互 | 可新增、重命名企业，存在部门或用户时禁止删除 | 静态检查通过 | 通过 |
| 跨企业部门校验 | 保存用户时拒绝部门与企业不匹配的数据 | 静态检查通过 | 通过 |

### 接口联调状态

| 接口 | 当前状态 | 后续工作 |
|---|---|---|
| `POST /api/admin/users/list` | 前端使用模拟数据 | 替换 `fetchUsers` 方法 |
| `POST /api/admin/users/add` | 前端使用本地新增逻辑 | 替换 `saveUser` 新增分支 |
| `POST /api/admin/users/update` | 前端使用本地更新逻辑 | 替换 `saveUser` 编辑分支 |
| `POST /api/admin/users/delete` | 前端使用本地删除逻辑 | 替换 `handleDelete` 方法 |
| `POST /api/admin/departments/list` | 前端使用模拟数据 | 替换 `fetchDepartments` 方法 |
| `POST /api/admin/departments/add` | 前端使用本地新增逻辑 | 替换 `saveDepartment` 新增分支 |
| `POST /api/admin/departments/update` | 前端使用本地重命名逻辑 | 替换 `saveDepartment` 编辑分支 |
| `POST /api/admin/departments/delete` | 前端使用本地删除逻辑 | 替换 `deleteDepartment` 方法 |
| 企业组织管理接口 | 后端尚未提供目标接口 | 按 `BACKEND_TODO.md` 实现并替换 `fetchEnterprises/saveEnterprise/deleteEnterprise` |

> 联调注意：现有后端更新和删除用户使用 `target_id`；现有用户接口尚未支持 `permissions` 持久化，权限字段需要在后续后端阶段单独设计。
>
> 组织关系注意：前端已按企业 1:N 部门、部门 1:N 用户实现；当前数据库和后端仍是独立部门模型，后续改造以 `BACKEND_TODO.md` 为准。

### 变更说明

- 新增管理员公共页面框架，统一 H5 顶部栏、左侧导航、移动端顶部栏、鉴权和退出登录。
- 工作台与用户管理页面已接入公共框架。
- 用户管理页面完成桌面表格与微信端卡片双布局。
- API 文档已补充用户管理页面需要的请求参数、返回字段和调用时机。
- 新增 `BACKEND_TODO.md`，集中记录全部前端完成后的后端改造与联调任务。
- 数据库 DDL：无变更。
- 后端代码：无变更。
